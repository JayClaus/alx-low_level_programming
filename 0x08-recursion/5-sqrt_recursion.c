#include "main.h"

int find_sqrt(int num, int root);
int _sqrt_recursion(int n);
/**
 * find_sqrt - finds the natural 2 root
 * @num: the number
 * @root: the root
 * Return: 0
 */
int find_sqrt(int num, int root)
{
	if ((root * root) == num)
		return (root);
}
